# Pins tool versions for local development and CI. Install mise: https://mise.jdx.dev
# Run `mise install` in the repo root to activate these versions locally.
[tools]
terragrunt = "0.93.1"
opentofu = "1.10.6"
