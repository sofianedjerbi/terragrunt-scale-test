# Terragrunt's local download and generated-file cache. Always safe to ignore.
.terragrunt-cache

# Unit configurations generated by Terragrunt from terragrunt.stack.hcl files.
# Only the stack file itself should be versioned, not what it generates.
.terragrunt-stack
